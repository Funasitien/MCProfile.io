version: '3.4'

services:
  mcprofile:
    image: mcprofile:latest
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 80:80
    volumes:
      - ./app:/app